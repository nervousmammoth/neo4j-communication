// Alternative layout.tsx using SYSTEM FONTS (no download required)
// This is the simplest option for on-prem deployments
//
// Pros:
// - No internet required
// - No fonts to download/manage
// - Instant builds
// - Smaller bundle size
//
// Cons:
// - Different look across operating systems
// - Less design consistency
//
// To use this configuration:
// 1. Rename this file to layout.tsx (backup the original first)
// 2. Update globals.css font-family declarations
// 3. Build and deploy

import type { Metadata } from "next";
import "./globals.css";
import { QueryClientProvider } from "@/lib/providers";
import { CommandPalette } from "@/components/command-palette";

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className="antialiased">
        <QueryClientProvider>
          <CommandPalette />
          {children}
        </QueryClientProvider>
      </body>
    </html>
  );
}
