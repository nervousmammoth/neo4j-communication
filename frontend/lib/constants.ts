/**
 * Shared constants used across the application
 */

/** LocalStorage key for storing user's view preference (list/card) */
export const CONVERSATIONS_VIEW_STORAGE_KEY = 'conversations-view'

/** Default view type for new users */
export const DEFAULT_VIEW_TYPE = 'card' as const